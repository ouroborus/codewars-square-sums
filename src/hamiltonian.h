// hamiltonian.h : Include file for standard system include files,
// or project specific include files.

#pragma once

#include <iostream>
#include <vector>
#include <set>
#include <string>
#include <algorithm>

#define cout std::cout
#define endl std::endl
#define flush std::flush
#define stoi std::atoi
#define reverse std::reverse

typedef unsigned int uint;
typedef std::vector<uint> vector;
typedef std::vector<std::vector<uint> > vector2;
typedef std::set<uint> set;
